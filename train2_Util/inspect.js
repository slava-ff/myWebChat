//var util = require('util');

var obj = {
  a: 5,
  b: 6,
  inspect: function() {
      return 123;
  }
};
//obj.self = obj;

//console.log( util.inspect(obj) );
console.log(obj);
//// не понял только для чего util.inspect, как ни меняю - ничего не меняется в консоль.лог
